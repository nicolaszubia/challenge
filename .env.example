# Optional. Spectra works fully without this key.
# When set, the server uses it to generate short explanations
# and recommendations for contrast findings.
# Never expose this value to the browser.
OPENAI_API_KEY=
